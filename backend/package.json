{
    "$schema": "https://json.schemastore.org/package.json",
    "name": "pdf-pulse-backend",
    "version": "1.0.0",
    "description": "Laravel backend for PDF Pulse application",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "chmod +x build.sh && ./build.sh && vite build",
        "dev": "vite",
        "start": "chmod +x start.sh && ./start.sh"
    },
    "engines": {
        "node": "18.x"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.4"
    }
}
